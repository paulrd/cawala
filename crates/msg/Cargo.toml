[package]
name = "cawala-msg"
version = "0.1.0"
edition = "2024"

[dependencies]
proto = { package = "cawala-proto", path = "../proto" }
serde = { version = "1.0.217", features = ["derive"] }
postcard = { version = "1.1.3", features = ["use-std"] }
thiserror = "2.0"
